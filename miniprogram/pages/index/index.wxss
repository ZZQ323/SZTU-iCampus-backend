.container {
  min-height: 100vh;
  background-color: #f5f5f5;
  padding-bottom: 40rpx;
}

.custom-navbar {
  background-color: #0052d9;
  color: #fff;
}

/* 头部样式 */
.header {
  background: linear-gradient(135deg, #0052d9 0%, #4a90e2 100%);
  padding: 40rpx 32rpx 32rpx;
  margin-bottom: 32rpx;
}

.user-card {
  display: flex;
  align-items: center;
  background: rgba(255, 255, 255, 0.95);
  border-radius: 16rpx;
  padding: 32rpx;
  box-shadow: 0 8rpx 32rpx rgba(0, 82, 217, 0.15);
}

.user-avatar {
  width: 120rpx;
  height: 120rpx;
  border-radius: 60rpx;
  margin-right: 24rpx;
  border: 4rpx solid #ffffff;
}

.user-info {
  flex: 1;
}

.user-name {
  display: block;
  font-size: 36rpx;
  font-weight: 600;
  color: #1a1a1a;
  margin-bottom: 8rpx;
}

.user-id {
  display: block;
  font-size: 28rpx;
  color: #666666;
}

/* 通用区块样式 */
.section {
  margin-bottom: 32rpx;
}

.section-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #1a1a1a;
  margin: 0 32rpx 24rpx;
}

/* 功能导航区样式 */
.feature-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 24rpx;
  padding: 0 32rpx;
}

.feature-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 24rpx 16rpx;
  background: #ffffff;
  border-radius: 16rpx;
  box-shadow: 0 2rpx 16rpx rgba(0, 0, 0, 0.06);
  transition: all 0.3s ease;
}

.feature-item:hover {
  transform: translateY(-4rpx);
  box-shadow: 0 8rpx 24rpx rgba(0, 0, 0, 0.12);
}

.feature-icon {
  width: 64rpx;
  height: 64rpx;
  margin-bottom: 12rpx;
}

.feature-text {
  font-size: 24rpx;
  color: #333333;
  text-align: center;
}

/* 快捷功能区样式 */
.quick-actions {
  margin: 0 32rpx;
  background: #ffffff;
  border-radius: 16rpx;
  overflow: hidden;
  box-shadow: 0 2rpx 16rpx rgba(0, 0, 0, 0.06);
}

/* 公告区样式 */
.announcement-section {
  margin: 0 32rpx;
  background: #ffffff;
  border-radius: 16rpx;
  overflow: hidden;
  box-shadow: 0 2rpx 16rpx rgba(0, 0, 0, 0.06);
}

/* 单元格样式 */
.t-cell {
  padding: 24rpx 32rpx;
  transition: background-color 0.2s ease;
}

.t-cell__title {
  font-size: 28rpx;
  color: #333;
}

.t-cell__note {
  font-size: 24rpx;
  color: #999;
}

/* 图标样式 */
.t-icon {
  font-size: 40rpx;
  color: #0052d9;
}

/* 加载状态样式 */
.loading-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 80rpx 32rpx;
  background: #ffffff;
  border-radius: 16rpx;
}

/* 空状态样式 */
.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 80rpx 32rpx;
  background: #f8f9fa;
}

.empty-text {
  font-size: 28rpx;
  color: #999999;
  text-align: center;
}

/* 公告项样式优化 */
.t-cell:active {
  background-color: #f5f5f5;
}

/* index.wxss - 流式数据体验首页样式 */

.index {
  min-height: 100vh;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  padding: 20rpx;
  padding-bottom: 120rpx;
}

/* 🌊 流式连接状态指示器 */
.stream-status {
  background: rgba(255, 255, 255, 0.9);
  border-radius: 20rpx;
  padding: 20rpx 30rpx;
  margin-bottom: 30rpx;
  display: flex;
  align-items: center;
  justify-content: space-between;
  box-shadow: 0 8rpx 32rpx rgba(0, 0, 0, 0.1);
  backdrop-filter: blur(10rpx);
  transition: all 0.3s ease;
}

.stream-status.connected {
  border-left: 6rpx solid #00a870;
}

.stream-status.disconnected {
  border-left: 6rpx solid #e34d59;
}

.status-indicator {
  display: flex;
  align-items: center;
  gap: 15rpx;
}

.status-dot {
  width: 20rpx;
  height: 20rpx;
  border-radius: 50%;
  background: #ccc;
  transition: all 0.3s ease;
}

.status-dot.active {
  background: #00a870;
  animation: pulse 2s infinite;
}

.status-text {
  font-size: 28rpx;
  font-weight: 600;
  color: #333;
}

.update-time {
  font-size: 24rpx;
  color: #666;
}

/* 用户信息卡片 */
.user-card {
  background: rgba(255, 255, 255, 0.95);
  border-radius: 24rpx;
  padding: 40rpx;
  margin-bottom: 40rpx;
  display: flex;
  align-items: center;
  gap: 30rpx;
  box-shadow: 0 12rpx 48rpx rgba(0, 0, 0, 0.15);
  backdrop-filter: blur(20rpx);
}

.user-avatar {
  width: 120rpx;
  height: 120rpx;
  background: linear-gradient(135deg, #667eea, #764ba2);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 8rpx 24rpx rgba(102, 126, 234, 0.3);
}

.user-icon {
  font-size: 60rpx;
}

.user-info {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.user-name {
  font-size: 36rpx;
  font-weight: 700;
  color: #333;
}

.user-details {
  font-size: 28rpx;
  color: #666;
}

.user-college {
  font-size: 24rpx;
  color: #999;
}

.user-status {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
}

.status-badge {
  padding: 8rpx 16rpx;
  border-radius: 12rpx;
  font-size: 24rpx;
  font-weight: 600;
}

.status-badge.online {
  background: rgba(0, 168, 112, 0.1);
  color: #00a870;
  border: 2rpx solid rgba(0, 168, 112, 0.2);
}

.status-badge.offline {
  background: rgba(227, 77, 89, 0.1);
  color: #e34d59;
  border: 2rpx solid rgba(227, 77, 89, 0.2);
}

/* 服务网格 */
.services-section {
  margin-bottom: 40rpx;
}

.section-header {
  display: flex;
  flex-direction: column;
  gap: 8rpx;
  margin-bottom: 30rpx;
}

/* .section-title {
  font-size: 32rpx;
  font-weight: 700;
  color: white;
} */

.section-subtitle {
  font-size: 24rpx;
  color: rgba(255, 255, 255, 0.8);
}

.services-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 20rpx;
}

.service-item {
  background: rgba(255, 255, 255, 0.9);
  border-radius: 20rpx;
  padding: 30rpx 20rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12rpx;
  position: relative;
  transition: all 0.3s ease;
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.1);
}

.service-item:active {
  transform: scale(0.95);
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.15);
}

.service-icon {
  font-size: 48rpx;
}

.service-title {
  font-size: 24rpx;
  font-weight: 600;
  color: #333;
  text-align: center;
}

.service-indicator {
  position: absolute;
  top: -8rpx;
  right: -8rpx;
  width: 36rpx;
  height: 36rpx;
  background: #e34d59;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  animation: bounce 2s infinite;
}

.new-count {
  font-size: 20rpx;
  font-weight: 700;
  color: white;
}

/* 公告区域 */
.announcements-section {
  background: rgba(255, 255, 255, 0.95);
  border-radius: 24rpx;
  padding: 30rpx;
  margin-bottom: 40rpx;
  box-shadow: 0 12rpx 48rpx rgba(0, 0, 0, 0.15);
  backdrop-filter: blur(20rpx);
}

.section-actions {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.stream-indicator {
  display: flex;
  align-items: center;
  padding: 8px 12px;
  border-radius: 20px;
  background: rgba(255, 255, 255, 0.1);
  transition: all 0.3s ease;
}

.stream-indicator.connected {
  background: rgba(0, 200, 83, 0.2);
}

.stream-indicator.disconnected {
  background: rgba(250, 81, 81, 0.2);
}

.stream-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: #fa5151;
  margin-right: 6px;
  transition: all 0.3s ease;
}

.stream-indicator.connected .stream-dot {
  background: #00c853;
}

.stream-dot.pulse {
  animation: streamPulse 2s infinite;
}

@keyframes streamPulse {
  0%, 100% { 
    opacity: 1; 
    transform: scale(1);
  }
  50% { 
    opacity: 0.7; 
    transform: scale(1.2);
  }
}

.stream-text {
  font-size: 12px;
  color: black;
  font-weight: 600;
}

.section-more {
  font-size: 26rpx;
  color: #0052d9;
  font-weight: 600;
}

/* 加载状态 */
.loading-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 20rpx;
  padding: 60rpx 0;
}

.loading-spinner {
  width: 60rpx;
  height: 60rpx;
  border: 4rpx solid rgba(0, 82, 217, 0.2);
  border-top: 4rpx solid #0052d9;
  border-radius: 50%;
  animation: spin 1s linear infinite;
}

.loading-text {
  font-size: 28rpx;
  color: #666;
}

/* 公告列表 */
.announcements-list {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.announcement-item {
  background: rgba(255, 255, 255, 0.8);
  border-radius: 16rpx;
  padding: 30rpx;
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  transition: all 0.3s ease;
  border-left: 4rpx solid transparent;
}

.announcement-item:active {
  transform: translateX(8rpx);
  border-left-color: #0052d9;
  box-shadow: 0 4rpx 16rpx rgba(0, 82, 217, 0.15);
}

.announcement-content {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 12rpx;
}

.announcement-header {
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.announcement-title {
  font-size: 28rpx;
  font-weight: 600;
  color: #333;
  line-height: 1.4;
}

.announcement-meta {
  display: flex;
  align-items: center;
  gap: 20rpx;
}

.announcement-department {
  font-size: 22rpx;
  color: #0052d9;
  font-weight: 600;
}

.announcement-time {
  font-size: 22rpx;
  color: #999;
}

.announcement-preview {
  font-size: 24rpx;
  color: #666;
  line-height: 1.4;
  overflow: hidden;
  text-overflow: ellipsis;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
}

.announcement-indicator {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12rpx;
}

.announcement-status.new {
  background: #e34d59;
  color: white;
  padding: 4rpx 8rpx;
  border-radius: 8rpx;
  font-size: 20rpx;
  font-weight: 600;
  animation: glow 2s infinite;
}

.announcement-priority {
  width: 12rpx;
  height: 12rpx;
  border-radius: 50%;
}

.priority-high {
  background: #e34d59;
}

.priority-medium {
  background: #ff8800;
}

.priority-low {
  background: #00a870;
}

/* 空状态 */
.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 16rpx;
  padding: 80rpx 0;
}

.empty-icon {
  font-size: 80rpx;
  opacity: 0.5;
}

.empty-text {
  font-size: 28rpx;
  color: #666;
  font-weight: 600;
}

.empty-subtitle {
  font-size: 24rpx;
  color: #999;
  text-align: center;
  line-height: 1.4;
}

/* 流式体验展示区域 */
.experience-section {
  background: rgba(255, 255, 255, 0.95);
  border-radius: 24rpx;
  padding: 30rpx;
  margin-bottom: 40rpx;
  box-shadow: 0 12rpx 48rpx rgba(0, 0, 0, 0.15);
  backdrop-filter: blur(20rpx);
}

.experience-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 20rpx;
  margin-bottom: 30rpx;
}

.experience-item {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  border-radius: 16rpx;
  padding: 30rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12rpx;
  color: white;
  text-align: center;
}

.experience-icon {
  font-size: 48rpx;
}

.experience-title {
  font-size: 26rpx;
  font-weight: 700;
}

.experience-desc {
  font-size: 22rpx;
  opacity: 0.9;
  line-height: 1.3;
}

.experience-stats {
  display: flex;
  justify-content: space-around;
  padding: 30rpx 0;
  background: rgba(102, 126, 234, 0.1);
  border-radius: 16rpx;
}

.stat-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
}

.stat-number {
  font-size: 48rpx;
  font-weight: 700;
  color: #0052d9;
}

.stat-label {
  font-size: 22rpx;
  color: #666;
  text-align: center;
}

/* 对比区域 */
.comparison-section {
  background: rgba(255, 255, 255, 0.95);
  border-radius: 24rpx;
  padding: 30rpx;
  margin-bottom: 40rpx;
  box-shadow: 0 12rpx 48rpx rgba(0, 0, 0, 0.15);
  backdrop-filter: blur(20rpx);
}

.comparison-table {
  display: flex;
  flex-direction: column;
  gap: 2rpx;
  background: #f5f5f5;
  border-radius: 12rpx;
  overflow: hidden;
}

.comparison-row {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  background: white;
}

.comparison-row.header {
  background: linear-gradient(135deg, #667eea, #764ba2);
}

.comparison-cell {
  padding: 20rpx 16rpx;
  font-size: 24rpx;
  text-align: center;
  display: flex;
  align-items: center;
  justify-content: center;
}

.comparison-row.header .comparison-cell {
  color: white;
  font-weight: 700;
}

.comparison-cell.traditional {
  color: #e34d59;
  font-weight: 600;
}

.comparison-cell.streaming {
  color: #00a870;
  font-weight: 600;
}

/* 浮动流式状态按钮 */
.floating-stream-button {
  position: fixed;
  bottom: 40rpx;
  right: 40rpx;
  width: 100rpx;
  height: 100rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, #667eea, #764ba2);
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 8rpx 32rpx rgba(102, 126, 234, 0.4);
  z-index: 1000;
  transition: all 0.3s ease;
}

.floating-stream-button:active {
  transform: scale(0.9);
}

.floating-stream-button.connected {
  animation: float 3s ease-in-out infinite;
}

.float-icon {
  font-size: 48rpx;
  color: white;
}

.float-pulse {
  position: absolute;
  width: 100%;
  height: 100%;
  border-radius: 50%;
  border: 4rpx solid rgba(255, 255, 255, 0.3);
}

.float-pulse.active {
  animation: pulse-ring 2s infinite;
}

/* 动画定义 */
@keyframes pulse {
  0%, 100% { opacity: 1; transform: scale(1); }
  50% { opacity: 0.7; transform: scale(1.1); }
}

@keyframes stream-pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.3; }
}

@keyframes spin {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}

@keyframes bounce {
  0%, 20%, 53%, 80%, 100% { transform: translate3d(0, 0, 0); }
  40%, 43% { transform: translate3d(0, -8rpx, 0); }
  70% { transform: translate3d(0, -4rpx, 0); }
  90% { transform: translate3d(0, -2rpx, 0); }
}

@keyframes glow {
  0%, 100% { box-shadow: 0 0 8rpx rgba(227, 77, 89, 0.5); }
  50% { box-shadow: 0 0 16rpx rgba(227, 77, 89, 0.8); }
}

@keyframes float {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-10rpx); }
}

@keyframes pulse-ring {
  0% { transform: scale(1); opacity: 1; }
  100% { transform: scale(1.5); opacity: 0; }
}

/* 响应式设计 */
@media (max-width: 480rpx) {
  .services-grid {
    grid-template-columns: repeat(3, 1fr);
  }
  
  .experience-grid {
    grid-template-columns: 1fr;
  }
}

/* 🎮 体验模式控制面板 */
.experience-panel {
  margin: 20px;
  padding: 20px;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  border-radius: 16px;
  box-shadow: 0 8px 32px rgba(102, 126, 234, 0.3);
  color: white;
  opacity: 0.9;
  transition: all 0.3s ease;
}

.experience-panel.active {
  opacity: 1;
  transform: translateY(-2px);
  box-shadow: 0 12px 40px rgba(102, 126, 234, 0.4);
}

.panel-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 20px;
}

.panel-title {
  font-size: 16px;
  font-weight: 700;
  color: white;
}

.demo-switch {
  transform: scale(0.8);
}

/* 📊 实时统计网格 */
.stats-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 16px;
  margin-bottom: 20px;
}

.stat-item {
  text-align: center;
  padding: 12px 8px;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 12px;
  backdrop-filter: blur(10px);
}

.stat-number {
  display: block;
  font-size: 18px;
  font-weight: 700;
  color: #ffd54f;
  margin-bottom: 4px;
}

.stat-label {
  font-size: 11px;
  color: rgba(255, 255, 255, 0.8);
  line-height: 1.2;
}

/* 🎯 体验操作按钮 */
.action-buttons {
  display: flex;
  gap: 8px;
}

.action-btn {
  flex: 1;
  padding: 10px 12px;
  border-radius: 8px;
  font-size: 12px;
  font-weight: 600;
  border: none;
  transition: all 0.2s ease;
}

.action-btn.primary {
  background: rgba(0, 82, 217, 0.8);
  color: white;
}

.action-btn.secondary {
  background: rgba(255, 255, 255, 0.2);
  color: white;
}

.action-btn.danger {
  background: rgba(250, 81, 81, 0.8);
  color: white;
}

.action-btn:active {
  transform: scale(0.95);
}

/* 🔗 用户卡片连接状态 */
.user-card {
  position: relative;
  /* ... existing styles ... */
}

.connection-status {
  position: absolute;
  top: 16px;
  right: 16px;
  text-align: right;
}

.status-badge {
  display: block;
  font-size: 12px;
  font-weight: 600;
  padding: 4px 8px;
  border-radius: 12px;
  margin-bottom: 4px;
}

.status-badge.online {
  background: rgba(0, 200, 83, 0.1);
  color: #00c853;
  border: 1px solid rgba(0, 200, 83, 0.3);
}

.status-badge.offline {
  background: rgba(158, 158, 158, 0.1);
  color: #9e9e9e;
  border: 1px solid rgba(158, 158, 158, 0.3);
}

.last-update {
  font-size: 10px;
  color: #666;
}

/* 📢 快速操作徽章 */
.quick-action {
  position: relative;
  /* ... existing styles ... */
}

.push-badge {
  position: absolute;
  top: -5px;
  right: -5px;
  min-width: 18px;
  height: 18px;
  background: #ff1744;
  border-radius: 9px;
  color: white;
  font-size: 10px;
  font-weight: 600;
  display: flex;
  align-items: center;
  justify-content: center;
  animation: badgePulse 2s infinite;
}

.activity-badge {
  position: absolute;
  top: -3px;
  right: -3px;
  padding: 2px 6px;
  background: #00c853;
  border-radius: 8px;
  color: white;
  font-size: 9px;
  font-weight: 600;
  animation: activityPulse 1.5s infinite ease-in-out;
}

@keyframes badgePulse {
  0%, 100% { 
    transform: scale(1);
    opacity: 1;
  }
  50% { 
    transform: scale(1.1);
    opacity: 0.9;
  }
}

@keyframes activityPulse {
  0%, 100% { 
    opacity: 1;
  }
  50% { 
    opacity: 0.6;
  }
}

/* 🌊 新推送公告特效 */
.notice-item {
  position: relative;
  /* ... existing styles ... */
  transition: all 0.3s ease;
}

.notice-item.new-push {
  background: linear-gradient(135deg, rgba(255, 193, 7, 0.1) 0%, rgba(255, 152, 0, 0.1) 100%);
  border: 2px solid rgba(255, 193, 7, 0.3);
  box-shadow: 0 4px 20px rgba(255, 193, 7, 0.2);
  animation: newPushGlow 2s ease-in-out;
}

@keyframes newPushGlow {
  0%, 100% {
    box-shadow: 0 4px 20px rgba(255, 193, 7, 0.2);
  }
  50% {
    box-shadow: 0 8px 30px rgba(255, 193, 7, 0.4);
    transform: translateY(-2px);
  }
}

.new-push-indicator {
  position: absolute;
  top: -10px;
  left: 16px;
  background: linear-gradient(45deg, #ff6b6b, #ffd93d);
  padding: 4px 10px;
  border-radius: 12px;
  z-index: 10;
}

.pulse-text {
  font-size: 10px;
  font-weight: 700;
  color: white;
  animation: textPulse 1s infinite ease-in-out;
}

@keyframes textPulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.8; }
}

/* 📊 数据来源标识 */
.source-badge {
  position: absolute;
  top: 12px;
  right: 12px;
}

.source-tag {
  font-size: 10px;
  font-weight: 600;
  padding: 3px 8px;
  border-radius: 10px;
  color: white;
}

.source-tag.realtime {
  background: linear-gradient(45deg, #00c853, #64dd17);
  animation: realtimePulse 2s infinite ease-in-out;
}

.source-tag.offline {
  background: linear-gradient(45deg, #9e9e9e, #757575);
}

.source-tag.normal {
  background: linear-gradient(45deg, #2196f3, #03a9f4);
}

@keyframes realtimePulse {
  0%, 100% { 
    opacity: 1;
    transform: scale(1);
  }
  50% { 
    opacity: 0.8;
    transform: scale(0.95);
  }
}

/* 👥 实时参与人数进度条 */
.participants-info {
  margin-top: 8px;
}

.participants-count {
  font-size: 12px;
  color: #666;
  font-weight: 600;
  margin-bottom: 4px;
}

.progress-bar {
  width: 100%;
  height: 4px;
  background: rgba(0, 82, 217, 0.1);
  border-radius: 2px;
  overflow: hidden;
  position: relative;
}

.progress-fill {
  height: 100%;
  background: linear-gradient(90deg, #0052d9, #1976d2);
  border-radius: 2px;
  transition: width 0.8s ease;
  position: relative;
}

.progress-fill::after {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
  animation: progressShine 2s infinite;
}

@keyframes progressShine {
  0% { transform: translateX(-100%); }
  100% { transform: translateX(100%); }
}

/* 🔄 加载状态优化 */
.empty-state {
  text-align: center;
  padding: 40px 20px;
  color: #999;
}

.empty-text {
  font-size: 14px;
  color: #999;
}

/* 📱 响应式优化 */
@media (max-width: 375px) {
  .stats-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 12px;
  }
  
  .stat-number {
    font-size: 16px;
  }
  
  .action-buttons {
    flex-direction: column;
    gap: 8px;
  }
  
  .action-btn {
    width: 100%;
  }
} 