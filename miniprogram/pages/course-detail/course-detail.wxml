<view class="course-detail">
  <view wx:if="{{loading}}" class="loading-container">
    <view class="loading-spinner"></view>
    <text class="loading-text">加载中...</text>
  </view>

  <view wx:else class="detail-container">
    <!-- 课程标题和基本信息 -->
    <view class="course-header">
      <text class="course-title">{{course.name}}</text>
      <view class="course-meta">
        <view class="meta-item">
          <text class="meta-label">授课教师</text>
          <text class="meta-value">{{course.teacher}}</text>
        </view>
        <view class="meta-item">
          <text class="meta-label">上课地点</text>
          <text class="meta-value">{{course.location}}</text>
        </view>
        <view class="meta-item">
          <text class="meta-label">课程类型</text>
          <text class="meta-value">{{course.typeText}}</text>
        </view>
        <view class="meta-item">
          <text class="meta-label">上课时间</text>
          <text class="meta-value">{{course.timeText}}</text>
        </view>
      </view>
    </view>

    <!-- 课程详细信息 -->
    <view class="course-content">
      <view class="content-section">
        <view class="section-title">📚 课程信息</view>
        <view class="info-grid">
          <view class="info-item">
            <text class="info-label">课程性质</text>
            <text class="info-value">{{course.typeText}}</text>
          </view>
          <view class="info-item" wx:if="{{course.credits}}">
            <text class="info-label">学分</text>
            <text class="info-value">{{course.credits}}</text>
          </view>
          <view class="info-item" wx:if="{{course.weeks}}">
            <text class="info-label">上课周次</text>
            <text class="info-value">{{course.weeks}}</text>
          </view>
          <view class="info-item" wx:if="{{course.classroom_type}}">
            <text class="info-label">教室类型</text>
            <text class="info-value">{{course.classroom_type}}</text>
          </view>
        </view>
      </view>

      <view class="content-section" wx:if="{{course.description}}">
        <view class="section-title">📝 课程描述</view>
        <text class="course-description">{{course.description}}</text>
      </view>

      <view class="content-section" wx:if="{{course.textbook}}">
        <view class="section-title">📖 教材信息</view>
        <text class="textbook-info">{{course.textbook}}</text>
      </view>

      <view class="content-section" wx:if="{{course.requirements}}">
        <view class="section-title">⚠️ 课程要求</view>
        <text class="requirements-text">{{course.requirements}}</text>
      </view>
    </view>

    <!-- 操作按钮 -->
    <view class="action-buttons">
      <t-button 
        theme="light" 
        size="large"
        bindtap="copyCourseInfo"
        icon="content-copy"
      >
        复制信息
      </t-button>
      <t-button 
        theme="primary" 
        size="large"
        bindtap="shareCourse"
        icon="share"
      >
        分享课程
      </t-button>
    </view>
  </view>
</view> 